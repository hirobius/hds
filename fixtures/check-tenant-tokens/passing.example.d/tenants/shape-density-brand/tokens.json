{
  "$schema": "../../hirobius.tenant-tokens.schema.json",
  "$description": "Passing fixture: role.radius / semantic.space.* / semantic.borderWidth.* overrides — the shape/density surface permitted by ADR-022 (#128). Proves these paths validate (R1: not primitive-tier; R5: exist in base).",
  "role": {
    "radius": {
      "$type": "dimension",
      "$value": { "value": 0, "unit": "px" },
      "$description": "Sharp corners — valid role-tier override."
    }
  },
  "semantic": {
    "space": {
      "component": {
        "padding": {
          "$value": { "value": 16, "unit": "px" },
          "$description": "Tightened padding — valid semantic-tier override, with a density-aware Compact mode.",
          "$extensions": {
            "com.figma.variables": {
              "modes": {
                "Compact": { "value": 8, "unit": "px" }
              }
            }
          }
        }
      }
    },
    "borderWidth": {
      "default": {
        "$value": { "value": 2, "unit": "px" },
        "$description": "Heavier hairline — valid semantic-tier override."
      }
    }
  }
}
