// Minimal stub for check-manifest-drift fixture (passing case).
// HdsButton is referenced in FRAME_TAGS and present in the manifest → no drift.
const FRAME_TAGS = new Set(['HdsButton']);
const TEXT_TAGS = new Set([]);
const INSTANCE_TAGS = new Set([]);
const ICON_TAGS = new Set([]);
