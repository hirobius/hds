---
'@hirobius/design-system': patch
---

Add the `primitive.size.interactive.minCompact` token (24px) and apply it to Tokenizer's remove button, so the button meets the WCAG 2.2 2.5.8 minimum target size (hds#287).
