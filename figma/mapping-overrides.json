{
  "$comment": "Figma assets whose code counterpart is NOT a top-level component, so no @figma JSDoc tag can point at them and scripts/check-figma-coverage.mjs would report them forever. Each entry is a deliberate statement that the asset IS mapped, just not the usual way. Keep this small: an entry is an admission that Figma and code model the same thing differently, and the better fix is usually to make them agree. Every entry needs a reason a reviewer can check.",
  "overrides": [
    {
      "nodeId": "128:14",
      "name": "CardProgress",
      "mapsTo": "Card.Progress",
      "source": "src/app/components/card.tsx",
      "reason": "Compound member of Card, exported as Card.Progress and not registered as its own componentSpec — no Card sub-part is (CardHeader, CardTitle, CardDescription, CardBody, CardFooter are all absent too). Figma publishes it as a standalone component because Figma has no compound-component concept."
    },
    {
      "nodeId": "128:18",
      "name": "CardMetric",
      "mapsTo": "Card.Metric",
      "source": "src/app/components/card.tsx",
      "reason": "Compound member of Card, exported as Card.Metric. Same shape as CardProgress above."
    }
  ]
}
