// url=https://www.figma.com/design/UNMAPPED/HdsRadio
// source=src/app/components/radio.tsx
// component=HdsRadio
//
// UNMAPPED: no Figma node URL is recorded for HdsRadio. Add `@figma <node-url>` to the
// component JSDoc in src/app/components/radio.tsx, then run `pnpm figma:connect:generate`.
// `pnpm figma:connect:check` lists every unmapped template.
//
// GENERATED by scripts/generate-code-connect.mjs from figma/code-connect.json
// (Figma properties, evidence: hds#73) and the cva variants in the source.
// Do not edit by hand: change the registry or the component, then regenerate.
import figma from 'figma';

const instance = figma.selectedInstance;

const label = instance.getString('Label');
const checked = instance.getEnum('Checked', {
  false: 'false',
  true: 'true',
});
const disabled = instance.getEnum('Disabled', {
  false: 'false',
  true: 'true',
});

export default {
  example: figma.code`<HdsRadio label="${label}"${checked === 'false' ? ' checked={false}' : checked === 'true' ? ' checked' : ''}${disabled === 'true' ? ' disabled' : ''} onChange={setChecked} />`,
  imports: ["import { HdsRadio } from '@hirobius/design-system'"],
  id: 'hds-radio',
  metadata: { nestable: true },
};
